# pinocchio itself is best installed via conda-forge (see README), not pip:
#   conda install -c conda-forge pinocchio=3.9.0
numpy>=1.23
scipy>=1.9
proxsuite>=0.6
pyyaml>=6.0

# No depth-sensor SDK is a hard dependency of this repo -- perception.py
# takes a plain point-cloud callback, so install whatever SDK your camera
# needs separately and wrap it yourself.
