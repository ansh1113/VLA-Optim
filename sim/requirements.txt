# Only needed for sim/run_sim_demo.py -- not a dependency of the filter
# itself, and not needed on the real robot.
mujoco>=3.1,<3.2  # pin: newer mujoco has dropped Python 3.9 wheels on macOS
imageio
